streamlit>=1.59
pandas>=3.0
# CORRECTION: this floor used to read numpy>=2.5 - confirmed live via a real
# `pip install -r requirements.txt` that no numpy release satisfying that
# exists yet (PyPI tops out at 2.4.x as of this fix), so a fresh install of
# this app failed outright with "No matching distribution found for
# numpy>=2.5" before a single line of app code ever ran. Nothing here
# actually needs 2.5+ specifically (just np.polyfit and basic array ops,
# both long-stable) - lowered to a real, currently-installable floor.
numpy>=2.0
requests>=2.34
# streamlit 1.59 pins pyarrow<25 internally (its own setup metadata) - the
# previous floor here (>=25.0) directly violated that and could resolve to
# a pyarrow whose compiled libarrow ABI doesn't match what streamlit's own
# compiled extensions expect. Confirmed as a REAL crash, not just a pip
# warning: hit a hard segfault in libarrow.so from inside a streamlit
# dataframe render with a mismatched pyarrow installed. Keep this upper
# bound in sync with whatever streamlit actually declares if streamlit is
# ever upgraded past 1.59.
pyarrow>=14.0,<25.0
matplotlib>=3.11
# Required on WINDOWS specifically, which is where this app actually runs
# (see run_app.bat): Python's stdlib `zoneinfo` reads the operating
# system's tz database, and Windows doesn't ship one - ZoneInfo() raises
# ZoneInfoNotFoundError there without this package. The Game Slate converts
# every tip time (and every game's calendar DATE) into Central through
# zoneinfo, so without this the whole tab falls back to UTC. A no-op on
# Linux/macOS, which have their own system tz database.
tzdata>=2025.1
